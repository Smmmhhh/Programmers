-- 코드를 입력하세요
SELECT      TO_NUMBER(A.HOUR) AS HOUR
        ,   COUNT(A.HOUR) AS COUNT
FROM 
(
    SELECT TO_CHAR(DATETIME, 'HH24') AS HOUR
    FROM ANIMAL_OUTS
)A
WHERE A.HOUR > 8 AND A.HOUR < 20
GROUP BY A.HOUR
ORDER BY A.HOUR