-- 코드를 입력하세요
SELECT      A.PRICE_GROUP
        ,   COUNT(A.PRICE_GROUP) AS PRODUCTS
FROM 
(
    SELECT FLOOR(PRICE / 10000) * 10000 AS PRICE_GROUP
    FROM PRODUCT 
)A
GROUP BY A.PRICE_GROUP
ORDER BY A.PRICE_GROUP